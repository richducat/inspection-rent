
# Environment secrets — never commit; keep only *.example templates
.env
.env.*
*.env
!.env.example
!*.env.example

# Local tooling only (playwright for tests/mobile-overflow-check.cjs); never publish it
node_modules/
